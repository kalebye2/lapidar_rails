<%= form_with(model: civil_estado, local: true) do |form| %>
  <% if civil_estado.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(civil_estado.errors.count, "error") %> prohibited this civil_estado from being saved:</h2>

      <ul>
        <% civil_estado.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :estado, style: "display: block" %>
    <%= form.text_field :estado %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
