<%= form_with(model: atendimento_local_tipo) do |form| %>
  <% if atendimento_local_tipo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(atendimento_local_tipo.errors.count, "error") %> prohibited this atendimento_local_tipo from being saved:</h2>

      <ul>
        <% atendimento_local_tipo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
