---
# Para ser usado com Pandoc
# convertendo para LaTeX ou PDF
# Caso queira saber mais visite https://pandoc.org
#
title: Anamnese adulto
author-meta: <%= @adulto_anamnese.profissional.descricao_completa %>
date-meta: <%= render_data_brasil @adulto_anamnese.data %>

# Formatação
# tire o "#" onde quiser mudar valor
papersize: a4
fontsize: 12pt
mainfont: Liberation Sans
#geometry:
#	- left=2cm
#	- right=2cm
#	- top=3cm
#	- bottom=4cm

header-includes:
- |
    ```{=latex}
    % Tire o símbolo "%" do início das linhas que você quiser usar uma regra
    %\usepackage{indentfirst} % para indentar o primeiro parágrafo como os seguintes

    %\setlength\parindent{2em} % recuo da primeira linha do parágrafo

    %\parskip=1.5em % alterar o espaço entre parágrafos
    %\renewcommand{\baselinestretch}{1.5} % alterar o espaço entre linhas

    % contar o número total de páginas do documento
    \usepackage{lastpage}

    % para cabeçalho e rodapé
    \usepackage{fancyhdr}
    \fancyhead[L]{} % coloque aqui cabeçalho à esquerda
    \fancyhead[R]{Anamnese adulto - <%= @adulto_anamnese.pessoa.nome_completo %>}
    \fancyfoot[L]{\footnotesize{
    E-mail: \href{mailto:<%= @adulto_anamnese.profissional.email %>}{<%= @adulto_anamnese.profissional.email %>}\\
    Fone: \href{tel:<%= @adulto_anamnese.profissional.pessoa.render_fone_link %>}{<%= @adulto_anamnese.profissional.pessoa.render_fone_local %>}
    }}
    \fancyfoot[C]{\thepage\ / \pageref{LastPage}}
    \pagestyle{fancy}

    % titulo
    \usepackage{titling}
    \renewcommand{\maketitle}{
    \begin{center}
    \huge\textbf\thetitle
    \end{center}
    }
    ```

---

<% @adulto_anamnese.dados.each do |k,v| %>
# <%= k&.to_s&.humanize %>

<% v.each do |k,v| %>

#### <%= k&.to_s&.humanize %>

<%== v&.to_s %>
<% end %>

<% end %>
