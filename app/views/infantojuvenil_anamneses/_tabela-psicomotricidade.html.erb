<% cl = "tabela-infantojuvenil-anamnese-psicomotricidade" %>
<% caminho_ajax = infantojuvenil_anamnese_path(psicomotricidade.infantojuvenil_anamnese, authenticity_token: form_authenticity_token.to_s) %>
<% usuario = usuario_atual == psicomotricidade.infantojuvenil_anamnese.profissional.usuario %>
<tr class="<%= cl %>">
  <th colspan="100%">Psicomotricidade</th>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-sorriu-meses" %>
  <th>Com quantos meses a criança sorriu?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[sorriu_meses]", value: psicomotricidade.sorriu_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.sorriu_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-dirigir-ativamente-pessoas-meses" %>
  <th>Com quantos meses a criança começou a se dirigir aos outros?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[dirigir_ativamente_pessoas_meses]", value: psicomotricidade.dirigir_ativamente_pessoas_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.dirigir_ativamente_pessoas_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-ergueu-cabeca-meses" %>
  <th>Com quantos meses a criança ergueu a cabeça?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[ergueu_cabeca_meses]", value: psicomotricidade.ergueu_cabeca_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.ergueu_cabeca_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-sentou-meses" %>
  <th>Com quantos meses a criança sentou?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[sentou_meses]", value: psicomotricidade.sentou_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.sentou_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-engatinhou-meses" %>
  <th>Com quantos meses a criança engatinhou?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[engatinhou_meses]", value: psicomotricidade.engatinhou_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.engatinhou_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-andou-meses" %>
  <th>Com quantos meses a criança andou?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[andou_meses]", value: psicomotricidade.andou_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.andou_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-denticao-meses" %>
  <th>Com quantos meses apareceu a dentição?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[denticao_meses]", value: psicomotricidade.denticao_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.denticao_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-canhoto" %>
  <th>É canhoto?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select name: "psicomotricidade[canhoto]", patch: caminho_ajax, select: "##{curr_id}", target: "##{curr_id}", swap: "outerHTML", value: psicomotricidade.canhoto %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.canhoto %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-controle-esfincter-diurno-meses" %>
  <th>Com quantos meses a criança teve controle do esfíncter (cocô) durante o dia?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[controle_esfincter_diurno_meses]", value: psicomotricidade.controle_esfincter_diurno_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.controle_esfincter_diurno_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-controle-esfincter-noturno-meses" %>
  <th>Com quantos meses a criança teve controle do esfíncter (cocô) durante a noite?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[controle_esfincter_noturno_meses]", value: psicomotricidade.controle_esfincter_noturno_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.controle_esfincter_noturno_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-controle-vesical-diurno-meses" %>
  <th>Com quantos meses a criança teve controle vesical (xixi) durante o dia?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[controle_vesical_diurno_meses]", value: psicomotricidade.controle_vesical_diurno_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.controle_vesical_diurno_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-controle-vesical-noturno-meses" %>
  <th>Com quantos meses a criança teve controle vesical (xixi) durante o dia?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_number name: "psicomotricidade[controle_vesical_noturno_meses]", value: psicomotricidade.controle_vesical_noturno_meses, select: "##{curr_id}", target: "##{curr_id}", patch: caminho_ajax, swap: "outerHTML", trigger: "change delay:500ms" %>
    <% else %>
      <%= deixar_no_plural psicomotricidade.controle_vesical_noturno_meses, 'mês', 'meses' %>
    <% end %>
  </td>
</tr>


<tr class="<%= cl %>">
  <% curr_id = "#{cl}-ensino-controle-esfincter" %>
  <th>Como foi ensinado o controle do esfíncter (cocô)?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_textarea texto: psicomotricidade.ensino_controle_esfincter, patch: caminho_ajax, target: "##{curr_id}", select: "##{curr_id}", swap: "outerHTML", name: "psicomotricidade[ensino_controle_esfincter]" %>
    <% else %>
      <%== markdown_to_html psicomotricidade.ensino_controle_esfincter %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-atitude-enurese" %>
  <th>Como era a atitude da criança diante da enurese (xixi na cama)?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_textarea texto: psicomotricidade.atitude_enurese, patch: caminho_ajax, target: "##{curr_id}", select: "##{curr_id}", swap: "outerHTML", name: "psicomotricidade[atitude_enurese]" %>
    <% else %>
      <%== markdown_to_html psicomotricidade.atitude_enurese %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-precis-ajuda-banheiro" %>
  <th>A criança precisa de ajuda para usar o banheiro?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select name: "psicomotricidade[precisa_ajuda_banheiro]", patch: caminho_ajax, select: "##{curr_id}", target: "##{curr_id}", swap: "outerHTML", value: psicomotricidade.precisa_ajuda_banheiro %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.precisa_ajuda_banheiro %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-toma-banho-sozinho" %>
  <th>A criança toma banho sozinha?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select value: psicomotricidade.toma_banho_sozinho, name: "psicomotricidade[toma_banho_sozinho]", patch: caminho_ajax, target: "##{curr_id}", select: "##{curr_id}", swap: "outerHTML" %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.toma_banho_sozinho %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-veste-sozinho" %>
  <th>A criança se veste sozinha?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select name: "psicomotricidade[veste_sozinho]", value: psicomotricidade.veste_sozinho, patch: caminho_ajax, select: "##{curr_id}", target: "##{curr_id}", swap: "outerHTML" %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.veste_sozinho %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-caia-muito" %>
  <th>A criança caía muito?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select name: "psicomotricidade[caia_muito]", value: psicomotricidade.caia_muito, patch: caminho_ajax, select: "##{curr_id}", target: "##{curr_id}", swap: "outerHTML" %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.caia_muito %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-conhecimento-lateralidade" %>
  <th>A criança tem conhecimento de lateralidade (esquerdo e direito)?</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_select name: "psicomotricidade[conhecimento_lateralidade]", value: psicomotricidade.conhecimento_lateralidade, patch: caminho_ajax, select: "##{curr_id}", target: "##{curr_id}", swap: "outerHTML" %>
    <% else %>
      <%= sim_ou_nao psicomotricidade.conhecimento_lateralidade %>
    <% end %>
  </td>
</tr>

<tr class="<%= cl %>">
  <% curr_id = "#{cl}-outras-consideracoes" %>
  <th>Outras considerações sobre a psicomotricidade</th>
  <td id="<%= curr_id %>">
    <% if usuario %>
      <%== hx_textarea texto: psicomotricidade.outras_consideracoes, name: "psicomotricidade[outras_consideracoes]", patch: caminho_ajax, target: "##{curr_id}", select: "##{curr_id}", swap: "outerHTML" %>
    <% else %>
      <%== markdown_to_html(psicomotricidade.outras_consideracoes, 'Nenhuma') %>
    <% end %>
  </td>
</tr>
